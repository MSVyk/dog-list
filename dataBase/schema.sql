CREATE DATABASE dogsDB;

use dogsDB;

CREATE TABLE dogsTable (

  id int NOT NULL AUTO_INCREMENT,
  name varchar(20) NOT NULL,
  description varchar(200) NOT NULL,
  PRIMARY KEY (ID)
);
-- CREATE TABLE catsTable (

--   id int NOT NULL AUTO_INCREMENT,
--   name varchar(20) NOT NULL,
--   description varchar(200) NOT NULL,
--   PRIMARY KEY (ID)
-- );
